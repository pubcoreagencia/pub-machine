# DAILY RECORD — 2026-09-16

## Direction consolidated
PUB Machine is the execution substrate for specialized Web, data acquisition and infrastructure capabilities discovered in external research.

## Allocated capabilities
- Coolify → infrastructure/deployment reference: servers, applications, databases/services, deployments, health, logs, rollback, backup.
- Maxun → structured web extraction, scraping/crawling, APIs, scheduling and data acquisition.
- Crawl4AI → crawler/browser runtime, normalization, Markdown/structured extraction, sessions, caching and deep crawling.
- Browser Use → browser-agent capabilities: navigation, click/type, extraction, screenshots, sessions and browser state.
- OpenHands → reference only for agent execution/session concepts, not a duplicate orchestration layer.

## Boundary
PUB Machine executes. PUB Neural stores/relates/institutionalizes resulting knowledge. ACP may request Machine capabilities through explicit interfaces/adapters, but specialized browser/crawler/infrastructure execution does not move into ClosedLoopEngine.

## Flow
`capture → normalize → structure → PUB Neural`

## Rule
Do not import whole external projects. Import capabilities. Avoid duplicate workers, databases, sessions or runtimes across project boundaries.
